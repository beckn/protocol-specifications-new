# NOTICE:

The contents of this folder will been moved to `protocol-specifications-v2/schema/`. Implementers are required to change any downstream references to these files. 

To ensure backward compatibility, a symlink of these files has been placed in the `protocol-specifications-v2/schema/` folder.